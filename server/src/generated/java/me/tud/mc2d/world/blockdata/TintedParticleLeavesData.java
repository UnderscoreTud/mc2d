package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasDistance;
import me.tud.mc2d.world.blockdata.properties.HasPersistent;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class TintedParticleLeavesData extends BlockData implements HasDistance, HasPersistent, HasWaterlogged {
    private static final int DISTANCE_RADIX = 7;

    private static final int PERSISTENT_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = DISTANCE_RADIX * PERSISTENT_RADIX * WATERLOGGED_RADIX - 1;

    private @Range(from = 1, to = 7) int distance = 7;

    private boolean persistent = false;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public TintedParticleLeavesData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 1, to = 7) int distance() {
        return this.distance;
    }

    @Override
    public void distance(@Range(from = 1, to = 7) int distance) {
        Preconditions.checkArgument(1 <= distance && distance <= 7, "Value must be in [1, 7], got " + distance);
        this.distance = distance;
    }

    private int distanceToIndex() {
        return this.distance;
    }

    private void distanceFromIndex(int index) {
        this.distance(index);
    }

    private void distanceFromString(String value) {
        this.distance(Integer.parseInt(value));
    }

    @Override
    public boolean persistent() {
        return this.persistent;
    }

    @Override
    public void persistent(boolean persistent) {
        this.persistent = persistent;
    }

    private int persistentToIndex() {
        return this.persistent ? 0 : 1;
    }

    private void persistentFromIndex(int index) {
        this.persistent(index == 0);
    }

    private void persistentFromString(String value) {
        this.persistent(Boolean.parseBoolean(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * DISTANCE_RADIX + distanceToIndex();
        id = id * PERSISTENT_RADIX + persistentToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        persistentFromIndex(id % PERSISTENT_RADIX); id /= PERSISTENT_RADIX;
        distanceFromIndex(id % DISTANCE_RADIX); id /= DISTANCE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("distance")) {
            distanceFromString(properties.get("distance"));
        }
        if (properties.containsKey("persistent")) {
            persistentFromString(properties.get("persistent"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "TintedParticleLeavesData[distance=" + String.valueOf(distance).toLowerCase(Locale.ENGLISH) + ";persistent=" + String.valueOf(persistent).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
