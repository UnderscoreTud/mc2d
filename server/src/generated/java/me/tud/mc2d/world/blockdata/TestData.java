package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class TestData extends BlockData {
    private static final int MODE_RADIX = 4;

    static final int MAX_OFFSET = MODE_RADIX - 1;

    private Mode mode = Mode.START;

    @ApiStatus.Internal
    public TestData(int startingID) {
        super(startingID);
    }

    public Mode mode() {
        return this.mode;
    }

    public void mode(Mode mode) {
        Preconditions.checkNotNull(mode, "mode");
        this.mode = mode;
    }

    private int modeToIndex() {
        return this.mode.ordinal();
    }

    private void modeFromIndex(int index) {
        this.mode(Mode.values()[index]);
    }

    private void modeFromString(String value) {
        this.mode(Mode.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * MODE_RADIX + modeToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        modeFromIndex(id % MODE_RADIX); id /= MODE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("mode")) {
            modeFromString(properties.get("mode"));
        }
    }

    @Override
    public String toString() {
        return "TestData[mode=" + String.valueOf(mode).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Mode {
        START,

        LOG,

        FAIL,

        ACCEPT;

        public static Mode parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "start" -> Mode.START;
                case "log" -> Mode.LOG;
                case "fail" -> Mode.FAIL;
                case "accept" -> Mode.ACCEPT;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
