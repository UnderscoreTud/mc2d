package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class TurtleEggData extends BlockData {
    private static final int EGGS_RADIX = 4;

    private static final int HATCH_RADIX = 3;

    static final int MAX_OFFSET = EGGS_RADIX * HATCH_RADIX - 1;

    private @Range(from = 1, to = 4) int eggs = 1;

    private @Range(from = 0, to = 2) int hatch = 0;

    @ApiStatus.Internal
    public TurtleEggData(int startingID) {
        super(startingID);
    }

    public @Range(from = 1, to = 4) int eggs() {
        return this.eggs;
    }

    public void eggs(@Range(from = 1, to = 4) int eggs) {
        Preconditions.checkArgument(1 <= eggs && eggs <= 4, "Value must be in [1, 4], got " + eggs);
        this.eggs = eggs;
    }

    private int eggsToIndex() {
        return this.eggs;
    }

    private void eggsFromIndex(int index) {
        this.eggs(index);
    }

    private void eggsFromString(String value) {
        this.eggs(Integer.parseInt(value));
    }

    public @Range(from = 0, to = 2) int hatch() {
        return this.hatch;
    }

    public void hatch(@Range(from = 0, to = 2) int hatch) {
        Preconditions.checkArgument(0 <= hatch && hatch <= 2, "Value must be in [0, 2], got " + hatch);
        this.hatch = hatch;
    }

    private int hatchToIndex() {
        return this.hatch;
    }

    private void hatchFromIndex(int index) {
        this.hatch(index);
    }

    private void hatchFromString(String value) {
        this.hatch(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * EGGS_RADIX + eggsToIndex();
        id = id * HATCH_RADIX + hatchToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        hatchFromIndex(id % HATCH_RADIX); id /= HATCH_RADIX;
        eggsFromIndex(id % EGGS_RADIX); id /= EGGS_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("eggs")) {
            eggsFromString(properties.get("eggs"));
        }
        if (properties.containsKey("hatch")) {
            hatchFromString(properties.get("hatch"));
        }
    }

    @Override
    public String toString() {
        return "TurtleEggData[eggs=" + String.valueOf(eggs).toLowerCase(Locale.ENGLISH) + ";hatch=" + String.valueOf(hatch).toLowerCase(Locale.ENGLISH) + "]";
    }
}
