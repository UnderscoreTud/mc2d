package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class CakeData extends BlockData {
    private static final int BITES_RADIX = 7;

    static final int MAX_OFFSET = BITES_RADIX - 1;

    private @Range(from = 0, to = 6) int bites = 0;

    @ApiStatus.Internal
    public CakeData(int startingID) {
        super(startingID);
    }

    public @Range(from = 0, to = 6) int bites() {
        return this.bites;
    }

    public void bites(@Range(from = 0, to = 6) int bites) {
        Preconditions.checkArgument(0 <= bites && bites <= 6, "Value must be in [0, 6], got " + bites);
        this.bites = bites;
    }

    private int bitesToIndex() {
        return this.bites;
    }

    private void bitesFromIndex(int index) {
        this.bites(index);
    }

    private void bitesFromString(String value) {
        this.bites(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * BITES_RADIX + bitesToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        bitesFromIndex(id % BITES_RADIX); id /= BITES_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("bites")) {
            bitesFromString(properties.get("bites"));
        }
    }

    @Override
    public String toString() {
        return "CakeData[bites=" + String.valueOf(bites).toLowerCase(Locale.ENGLISH) + "]";
    }
}
