package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasAge;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class BambooStalkData extends BlockData implements HasAge {
    private static final int AGE_RADIX = 2;

    private static final int LEAVES_RADIX = 3;

    private static final int STAGE_RADIX = 2;

    static final int MAX_OFFSET = AGE_RADIX * LEAVES_RADIX * STAGE_RADIX - 1;

    private @Range(from = 0, to = 1) int age = 0;

    private Leaves leaves = Leaves.NONE;

    private @Range(from = 0, to = 1) int stage = 0;

    @ApiStatus.Internal
    public BambooStalkData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 0, to = 1) int age() {
        return this.age;
    }

    @Override
    public void age(@Range(from = 0, to = 1) int age) {
        Preconditions.checkArgument(0 <= age && age <= 1, "Value must be in [0, 1], got " + age);
        this.age = age;
    }

    private int ageToIndex() {
        return this.age;
    }

    private void ageFromIndex(int index) {
        this.age(index);
    }

    private void ageFromString(String value) {
        this.age(Integer.parseInt(value));
    }

    public Leaves leaves() {
        return this.leaves;
    }

    public void leaves(Leaves leaves) {
        Preconditions.checkNotNull(leaves, "leaves");
        this.leaves = leaves;
    }

    private int leavesToIndex() {
        return this.leaves.ordinal();
    }

    private void leavesFromIndex(int index) {
        this.leaves(Leaves.values()[index]);
    }

    private void leavesFromString(String value) {
        this.leaves(Leaves.parse(value));
    }

    public @Range(from = 0, to = 1) int stage() {
        return this.stage;
    }

    public void stage(@Range(from = 0, to = 1) int stage) {
        Preconditions.checkArgument(0 <= stage && stage <= 1, "Value must be in [0, 1], got " + stage);
        this.stage = stage;
    }

    private int stageToIndex() {
        return this.stage;
    }

    private void stageFromIndex(int index) {
        this.stage(index);
    }

    private void stageFromString(String value) {
        this.stage(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * AGE_RADIX + ageToIndex();
        id = id * LEAVES_RADIX + leavesToIndex();
        id = id * STAGE_RADIX + stageToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        stageFromIndex(id % STAGE_RADIX); id /= STAGE_RADIX;
        leavesFromIndex(id % LEAVES_RADIX); id /= LEAVES_RADIX;
        ageFromIndex(id % AGE_RADIX); id /= AGE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("age")) {
            ageFromString(properties.get("age"));
        }
        if (properties.containsKey("leaves")) {
            leavesFromString(properties.get("leaves"));
        }
        if (properties.containsKey("stage")) {
            stageFromString(properties.get("stage"));
        }
    }

    @Override
    public String toString() {
        return "BambooStalkData[age=" + String.valueOf(age).toLowerCase(Locale.ENGLISH) + ";leaves=" + String.valueOf(leaves).toLowerCase(Locale.ENGLISH) + ";stage=" + String.valueOf(stage).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Leaves {
        NONE,

        SMALL,

        LARGE;

        public static Leaves parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "none" -> Leaves.NONE;
                case "small" -> Leaves.SMALL;
                case "large" -> Leaves.LARGE;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
