package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class FarmData extends BlockData {
    private static final int MOISTURE_RADIX = 8;

    static final int MAX_OFFSET = MOISTURE_RADIX - 1;

    private @Range(from = 0, to = 7) int moisture = 0;

    @ApiStatus.Internal
    public FarmData(int startingID) {
        super(startingID);
    }

    public @Range(from = 0, to = 7) int moisture() {
        return this.moisture;
    }

    public void moisture(@Range(from = 0, to = 7) int moisture) {
        Preconditions.checkArgument(0 <= moisture && moisture <= 7, "Value must be in [0, 7], got " + moisture);
        this.moisture = moisture;
    }

    private int moistureToIndex() {
        return this.moisture;
    }

    private void moistureFromIndex(int index) {
        this.moisture(index);
    }

    private void moistureFromString(String value) {
        this.moisture(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * MOISTURE_RADIX + moistureToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        moistureFromIndex(id % MOISTURE_RADIX); id /= MOISTURE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("moisture")) {
            moistureFromString(properties.get("moisture"));
        }
    }

    @Override
    public String toString() {
        return "FarmData[moisture=" + String.valueOf(moisture).toLowerCase(Locale.ENGLISH) + "]";
    }
}
