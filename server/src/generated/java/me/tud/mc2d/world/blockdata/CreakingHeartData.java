package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class CreakingHeartData extends BlockData {
    private static final int AXIS_RADIX = 3;

    private static final int CREAKING_HEART_STATE_RADIX = 3;

    private static final int NATURAL_RADIX = 2;

    static final int MAX_OFFSET = AXIS_RADIX * CREAKING_HEART_STATE_RADIX * NATURAL_RADIX - 1;

    private Axis axis = Axis.Y;

    private CreakingHeartState creakingHeartState = CreakingHeartState.UPROOTED;

    private boolean natural = false;

    @ApiStatus.Internal
    public CreakingHeartData(int startingID) {
        super(startingID);
    }

    public Axis axis() {
        return this.axis;
    }

    public void axis(Axis axis) {
        Preconditions.checkNotNull(axis, "axis");
        this.axis = axis;
    }

    private int axisToIndex() {
        return this.axis.ordinal();
    }

    private void axisFromIndex(int index) {
        this.axis(Axis.values()[index]);
    }

    private void axisFromString(String value) {
        this.axis(Axis.parse(value));
    }

    public CreakingHeartState creakingHeartState() {
        return this.creakingHeartState;
    }

    public void creakingHeartState(CreakingHeartState creakingHeartState) {
        Preconditions.checkNotNull(creakingHeartState, "creakingHeartState");
        this.creakingHeartState = creakingHeartState;
    }

    private int creakingHeartStateToIndex() {
        return this.creakingHeartState.ordinal();
    }

    private void creakingHeartStateFromIndex(int index) {
        this.creakingHeartState(CreakingHeartState.values()[index]);
    }

    private void creakingHeartStateFromString(String value) {
        this.creakingHeartState(CreakingHeartState.parse(value));
    }

    public boolean natural() {
        return this.natural;
    }

    public void natural(boolean natural) {
        this.natural = natural;
    }

    private int naturalToIndex() {
        return this.natural ? 0 : 1;
    }

    private void naturalFromIndex(int index) {
        this.natural(index == 0);
    }

    private void naturalFromString(String value) {
        this.natural(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * AXIS_RADIX + axisToIndex();
        id = id * CREAKING_HEART_STATE_RADIX + creakingHeartStateToIndex();
        id = id * NATURAL_RADIX + naturalToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        naturalFromIndex(id % NATURAL_RADIX); id /= NATURAL_RADIX;
        creakingHeartStateFromIndex(id % CREAKING_HEART_STATE_RADIX); id /= CREAKING_HEART_STATE_RADIX;
        axisFromIndex(id % AXIS_RADIX); id /= AXIS_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("axis")) {
            axisFromString(properties.get("axis"));
        }
        if (properties.containsKey("creaking_heart_state")) {
            creakingHeartStateFromString(properties.get("creaking_heart_state"));
        }
        if (properties.containsKey("natural")) {
            naturalFromString(properties.get("natural"));
        }
    }

    @Override
    public String toString() {
        return "CreakingHeartData[axis=" + String.valueOf(axis).toLowerCase(Locale.ENGLISH) + ";creaking_heart_state=" + String.valueOf(creakingHeartState).toLowerCase(Locale.ENGLISH) + ";natural=" + String.valueOf(natural).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Axis {
        X,

        Y,

        Z;

        public static Axis parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "x" -> Axis.X;
                case "y" -> Axis.Y;
                case "z" -> Axis.Z;
                default -> throw new IllegalArgumentException();
            };
        }
    }

    public enum CreakingHeartState {
        UPROOTED,

        DORMANT,

        AWAKE;

        public static CreakingHeartState parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "uprooted" -> CreakingHeartState.UPROOTED;
                case "dormant" -> CreakingHeartState.DORMANT;
                case "awake" -> CreakingHeartState.AWAKE;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
