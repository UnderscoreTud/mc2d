package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasLit;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class RedstoneLampData extends BlockData implements HasLit {
    private static final int LIT_RADIX = 2;

    static final int MAX_OFFSET = LIT_RADIX - 1;

    private boolean lit = false;

    @ApiStatus.Internal
    public RedstoneLampData(int startingID) {
        super(startingID);
    }

    @Override
    public boolean lit() {
        return this.lit;
    }

    @Override
    public void lit(boolean lit) {
        this.lit = lit;
    }

    private int litToIndex() {
        return this.lit ? 0 : 1;
    }

    private void litFromIndex(int index) {
        this.lit(index == 0);
    }

    private void litFromString(String value) {
        this.lit(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * LIT_RADIX + litToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        litFromIndex(id % LIT_RADIX); id /= LIT_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("lit")) {
            litFromString(properties.get("lit"));
        }
    }

    @Override
    public String toString() {
        return "RedstoneLampData[lit=" + String.valueOf(lit).toLowerCase(Locale.ENGLISH) + "]";
    }
}
