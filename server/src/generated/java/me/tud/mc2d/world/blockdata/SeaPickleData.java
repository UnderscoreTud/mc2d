package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class SeaPickleData extends BlockData implements HasWaterlogged {
    private static final int PICKLES_RADIX = 4;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = PICKLES_RADIX * WATERLOGGED_RADIX - 1;

    private @Range(from = 1, to = 4) int pickles = 1;

    private boolean waterlogged = true;

    @ApiStatus.Internal
    public SeaPickleData(int startingID) {
        super(startingID);
    }

    public @Range(from = 1, to = 4) int pickles() {
        return this.pickles;
    }

    public void pickles(@Range(from = 1, to = 4) int pickles) {
        Preconditions.checkArgument(1 <= pickles && pickles <= 4, "Value must be in [1, 4], got " + pickles);
        this.pickles = pickles;
    }

    private int picklesToIndex() {
        return this.pickles;
    }

    private void picklesFromIndex(int index) {
        this.pickles(index);
    }

    private void picklesFromString(String value) {
        this.pickles(Integer.parseInt(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * PICKLES_RADIX + picklesToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        picklesFromIndex(id % PICKLES_RADIX); id /= PICKLES_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("pickles")) {
            picklesFromString(properties.get("pickles"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "SeaPickleData[pickles=" + String.valueOf(pickles).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
