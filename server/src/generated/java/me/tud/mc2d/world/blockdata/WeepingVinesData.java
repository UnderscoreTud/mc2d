package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasAge;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class WeepingVinesData extends BlockData implements HasAge {
    private static final int AGE_RADIX = 26;

    static final int MAX_OFFSET = AGE_RADIX - 1;

    private @Range(from = 0, to = 25) int age = 0;

    @ApiStatus.Internal
    public WeepingVinesData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 0, to = 25) int age() {
        return this.age;
    }

    @Override
    public void age(@Range(from = 0, to = 25) int age) {
        Preconditions.checkArgument(0 <= age && age <= 25, "Value must be in [0, 25], got " + age);
        this.age = age;
    }

    private int ageToIndex() {
        return this.age;
    }

    private void ageFromIndex(int index) {
        this.age(index);
    }

    private void ageFromString(String value) {
        this.age(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * AGE_RADIX + ageToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        ageFromIndex(id % AGE_RADIX); id /= AGE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("age")) {
            ageFromString(properties.get("age"));
        }
    }

    @Override
    public String toString() {
        return "WeepingVinesData[age=" + String.valueOf(age).toLowerCase(Locale.ENGLISH) + "]";
    }
}
