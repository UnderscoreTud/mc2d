package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasLit;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class CandleData extends BlockData implements HasLit, HasWaterlogged {
    private static final int CANDLES_RADIX = 4;

    private static final int LIT_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = CANDLES_RADIX * LIT_RADIX * WATERLOGGED_RADIX - 1;

    private @Range(from = 1, to = 4) int candles = 1;

    private boolean lit = false;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public CandleData(int startingID) {
        super(startingID);
    }

    public @Range(from = 1, to = 4) int candles() {
        return this.candles;
    }

    public void candles(@Range(from = 1, to = 4) int candles) {
        Preconditions.checkArgument(1 <= candles && candles <= 4, "Value must be in [1, 4], got " + candles);
        this.candles = candles;
    }

    private int candlesToIndex() {
        return this.candles;
    }

    private void candlesFromIndex(int index) {
        this.candles(index);
    }

    private void candlesFromString(String value) {
        this.candles(Integer.parseInt(value));
    }

    @Override
    public boolean lit() {
        return this.lit;
    }

    @Override
    public void lit(boolean lit) {
        this.lit = lit;
    }

    private int litToIndex() {
        return this.lit ? 0 : 1;
    }

    private void litFromIndex(int index) {
        this.lit(index == 0);
    }

    private void litFromString(String value) {
        this.lit(Boolean.parseBoolean(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * CANDLES_RADIX + candlesToIndex();
        id = id * LIT_RADIX + litToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        litFromIndex(id % LIT_RADIX); id /= LIT_RADIX;
        candlesFromIndex(id % CANDLES_RADIX); id /= CANDLES_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("candles")) {
            candlesFromString(properties.get("candles"));
        }
        if (properties.containsKey("lit")) {
            litFromString(properties.get("lit"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "CandleData[candles=" + String.valueOf(candles).toLowerCase(Locale.ENGLISH) + ";lit=" + String.valueOf(lit).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
