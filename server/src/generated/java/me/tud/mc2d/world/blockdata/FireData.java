package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasAge;
import me.tud.mc2d.world.blockdata.properties.HasUp;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class FireData extends BlockData implements HasAge, HasUp {
    private static final int AGE_RADIX = 16;

    private static final int EAST_RADIX = 2;

    private static final int NORTH_RADIX = 2;

    private static final int SOUTH_RADIX = 2;

    private static final int UP_RADIX = 2;

    private static final int WEST_RADIX = 2;

    static final int MAX_OFFSET = AGE_RADIX * EAST_RADIX * NORTH_RADIX * SOUTH_RADIX * UP_RADIX * WEST_RADIX - 1;

    private @Range(from = 0, to = 15) int age = 0;

    private boolean east = false;

    private boolean north = false;

    private boolean south = false;

    private boolean up = false;

    private boolean west = false;

    @ApiStatus.Internal
    public FireData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 0, to = 15) int age() {
        return this.age;
    }

    @Override
    public void age(@Range(from = 0, to = 15) int age) {
        Preconditions.checkArgument(0 <= age && age <= 15, "Value must be in [0, 15], got " + age);
        this.age = age;
    }

    private int ageToIndex() {
        return this.age;
    }

    private void ageFromIndex(int index) {
        this.age(index);
    }

    private void ageFromString(String value) {
        this.age(Integer.parseInt(value));
    }

    public boolean east() {
        return this.east;
    }

    public void east(boolean east) {
        this.east = east;
    }

    private int eastToIndex() {
        return this.east ? 0 : 1;
    }

    private void eastFromIndex(int index) {
        this.east(index == 0);
    }

    private void eastFromString(String value) {
        this.east(Boolean.parseBoolean(value));
    }

    public boolean north() {
        return this.north;
    }

    public void north(boolean north) {
        this.north = north;
    }

    private int northToIndex() {
        return this.north ? 0 : 1;
    }

    private void northFromIndex(int index) {
        this.north(index == 0);
    }

    private void northFromString(String value) {
        this.north(Boolean.parseBoolean(value));
    }

    public boolean south() {
        return this.south;
    }

    public void south(boolean south) {
        this.south = south;
    }

    private int southToIndex() {
        return this.south ? 0 : 1;
    }

    private void southFromIndex(int index) {
        this.south(index == 0);
    }

    private void southFromString(String value) {
        this.south(Boolean.parseBoolean(value));
    }

    @Override
    public boolean up() {
        return this.up;
    }

    @Override
    public void up(boolean up) {
        this.up = up;
    }

    private int upToIndex() {
        return this.up ? 0 : 1;
    }

    private void upFromIndex(int index) {
        this.up(index == 0);
    }

    private void upFromString(String value) {
        this.up(Boolean.parseBoolean(value));
    }

    public boolean west() {
        return this.west;
    }

    public void west(boolean west) {
        this.west = west;
    }

    private int westToIndex() {
        return this.west ? 0 : 1;
    }

    private void westFromIndex(int index) {
        this.west(index == 0);
    }

    private void westFromString(String value) {
        this.west(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * AGE_RADIX + ageToIndex();
        id = id * EAST_RADIX + eastToIndex();
        id = id * NORTH_RADIX + northToIndex();
        id = id * SOUTH_RADIX + southToIndex();
        id = id * UP_RADIX + upToIndex();
        id = id * WEST_RADIX + westToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        westFromIndex(id % WEST_RADIX); id /= WEST_RADIX;
        upFromIndex(id % UP_RADIX); id /= UP_RADIX;
        southFromIndex(id % SOUTH_RADIX); id /= SOUTH_RADIX;
        northFromIndex(id % NORTH_RADIX); id /= NORTH_RADIX;
        eastFromIndex(id % EAST_RADIX); id /= EAST_RADIX;
        ageFromIndex(id % AGE_RADIX); id /= AGE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("age")) {
            ageFromString(properties.get("age"));
        }
        if (properties.containsKey("east")) {
            eastFromString(properties.get("east"));
        }
        if (properties.containsKey("north")) {
            northFromString(properties.get("north"));
        }
        if (properties.containsKey("south")) {
            southFromString(properties.get("south"));
        }
        if (properties.containsKey("up")) {
            upFromString(properties.get("up"));
        }
        if (properties.containsKey("west")) {
            westFromString(properties.get("west"));
        }
    }

    @Override
    public String toString() {
        return "FireData[age=" + String.valueOf(age).toLowerCase(Locale.ENGLISH) + ";east=" + String.valueOf(east).toLowerCase(Locale.ENGLISH) + ";north=" + String.valueOf(north).toLowerCase(Locale.ENGLISH) + ";south=" + String.valueOf(south).toLowerCase(Locale.ENGLISH) + ";up=" + String.valueOf(up).toLowerCase(Locale.ENGLISH) + ";west=" + String.valueOf(west).toLowerCase(Locale.ENGLISH) + "]";
    }
}
