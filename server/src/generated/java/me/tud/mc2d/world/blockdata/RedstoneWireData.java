package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasPower;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class RedstoneWireData extends BlockData implements HasPower {
    private static final int EAST_RADIX = 3;

    private static final int NORTH_RADIX = 3;

    private static final int POWER_RADIX = 16;

    private static final int SOUTH_RADIX = 3;

    private static final int WEST_RADIX = 3;

    static final int MAX_OFFSET = EAST_RADIX * NORTH_RADIX * POWER_RADIX * SOUTH_RADIX * WEST_RADIX - 1;

    private East east = East.NONE;

    private North north = North.NONE;

    private @Range(from = 0, to = 15) int power = 0;

    private South south = South.NONE;

    private West west = West.NONE;

    @ApiStatus.Internal
    public RedstoneWireData(int startingID) {
        super(startingID);
    }

    public East east() {
        return this.east;
    }

    public void east(East east) {
        Preconditions.checkNotNull(east, "east");
        this.east = east;
    }

    private int eastToIndex() {
        return this.east.ordinal();
    }

    private void eastFromIndex(int index) {
        this.east(East.values()[index]);
    }

    private void eastFromString(String value) {
        this.east(East.parse(value));
    }

    public North north() {
        return this.north;
    }

    public void north(North north) {
        Preconditions.checkNotNull(north, "north");
        this.north = north;
    }

    private int northToIndex() {
        return this.north.ordinal();
    }

    private void northFromIndex(int index) {
        this.north(North.values()[index]);
    }

    private void northFromString(String value) {
        this.north(North.parse(value));
    }

    @Override
    public @Range(from = 0, to = 15) int power() {
        return this.power;
    }

    @Override
    public void power(@Range(from = 0, to = 15) int power) {
        Preconditions.checkArgument(0 <= power && power <= 15, "Value must be in [0, 15], got " + power);
        this.power = power;
    }

    private int powerToIndex() {
        return this.power;
    }

    private void powerFromIndex(int index) {
        this.power(index);
    }

    private void powerFromString(String value) {
        this.power(Integer.parseInt(value));
    }

    public South south() {
        return this.south;
    }

    public void south(South south) {
        Preconditions.checkNotNull(south, "south");
        this.south = south;
    }

    private int southToIndex() {
        return this.south.ordinal();
    }

    private void southFromIndex(int index) {
        this.south(South.values()[index]);
    }

    private void southFromString(String value) {
        this.south(South.parse(value));
    }

    public West west() {
        return this.west;
    }

    public void west(West west) {
        Preconditions.checkNotNull(west, "west");
        this.west = west;
    }

    private int westToIndex() {
        return this.west.ordinal();
    }

    private void westFromIndex(int index) {
        this.west(West.values()[index]);
    }

    private void westFromString(String value) {
        this.west(West.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * EAST_RADIX + eastToIndex();
        id = id * NORTH_RADIX + northToIndex();
        id = id * POWER_RADIX + powerToIndex();
        id = id * SOUTH_RADIX + southToIndex();
        id = id * WEST_RADIX + westToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        westFromIndex(id % WEST_RADIX); id /= WEST_RADIX;
        southFromIndex(id % SOUTH_RADIX); id /= SOUTH_RADIX;
        powerFromIndex(id % POWER_RADIX); id /= POWER_RADIX;
        northFromIndex(id % NORTH_RADIX); id /= NORTH_RADIX;
        eastFromIndex(id % EAST_RADIX); id /= EAST_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("east")) {
            eastFromString(properties.get("east"));
        }
        if (properties.containsKey("north")) {
            northFromString(properties.get("north"));
        }
        if (properties.containsKey("power")) {
            powerFromString(properties.get("power"));
        }
        if (properties.containsKey("south")) {
            southFromString(properties.get("south"));
        }
        if (properties.containsKey("west")) {
            westFromString(properties.get("west"));
        }
    }

    @Override
    public String toString() {
        return "RedstoneWireData[east=" + String.valueOf(east).toLowerCase(Locale.ENGLISH) + ";north=" + String.valueOf(north).toLowerCase(Locale.ENGLISH) + ";power=" + String.valueOf(power).toLowerCase(Locale.ENGLISH) + ";south=" + String.valueOf(south).toLowerCase(Locale.ENGLISH) + ";west=" + String.valueOf(west).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum East {
        UP,

        SIDE,

        NONE;

        public static East parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "up" -> East.UP;
                case "side" -> East.SIDE;
                case "none" -> East.NONE;
                default -> throw new IllegalArgumentException();
            };
        }
    }

    public enum North {
        UP,

        SIDE,

        NONE;

        public static North parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "up" -> North.UP;
                case "side" -> North.SIDE;
                case "none" -> North.NONE;
                default -> throw new IllegalArgumentException();
            };
        }
    }

    public enum South {
        UP,

        SIDE,

        NONE;

        public static South parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "up" -> South.UP;
                case "side" -> South.SIDE;
                case "none" -> South.NONE;
                default -> throw new IllegalArgumentException();
            };
        }
    }

    public enum West {
        UP,

        SIDE,

        NONE;

        public static West parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "up" -> West.UP;
                case "side" -> West.SIDE;
                case "none" -> West.NONE;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
