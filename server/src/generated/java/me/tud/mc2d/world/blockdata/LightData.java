package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasLevel;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class LightData extends BlockData implements HasLevel, HasWaterlogged {
    private static final int LEVEL_RADIX = 16;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = LEVEL_RADIX * WATERLOGGED_RADIX - 1;

    private @Range(from = 0, to = 15) int level = 15;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public LightData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 0, to = 15) int level() {
        return this.level;
    }

    @Override
    public void level(@Range(from = 0, to = 15) int level) {
        Preconditions.checkArgument(0 <= level && level <= 15, "Value must be in [0, 15], got " + level);
        this.level = level;
    }

    private int levelToIndex() {
        return this.level;
    }

    private void levelFromIndex(int index) {
        this.level(index);
    }

    private void levelFromString(String value) {
        this.level(Integer.parseInt(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * LEVEL_RADIX + levelToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        levelFromIndex(id % LEVEL_RADIX); id /= LEVEL_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("level")) {
            levelFromString(properties.get("level"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "LightData[level=" + String.valueOf(level).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
