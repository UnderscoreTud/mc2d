package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class SnowLayerData extends BlockData {
    private static final int LAYERS_RADIX = 8;

    static final int MAX_OFFSET = LAYERS_RADIX - 1;

    private @Range(from = 1, to = 8) int layers = 1;

    @ApiStatus.Internal
    public SnowLayerData(int startingID) {
        super(startingID);
    }

    public @Range(from = 1, to = 8) int layers() {
        return this.layers;
    }

    public void layers(@Range(from = 1, to = 8) int layers) {
        Preconditions.checkArgument(1 <= layers && layers <= 8, "Value must be in [1, 8], got " + layers);
        this.layers = layers;
    }

    private int layersToIndex() {
        return this.layers;
    }

    private void layersFromIndex(int index) {
        this.layers(index);
    }

    private void layersFromString(String value) {
        this.layers(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * LAYERS_RADIX + layersToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        layersFromIndex(id % LAYERS_RADIX); id /= LAYERS_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("layers")) {
            layersFromString(properties.get("layers"));
        }
    }

    @Override
    public String toString() {
        return "SnowLayerData[layers=" + String.valueOf(layers).toLowerCase(Locale.ENGLISH) + "]";
    }
}
