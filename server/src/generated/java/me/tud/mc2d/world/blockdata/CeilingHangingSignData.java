package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasAttached;
import me.tud.mc2d.world.blockdata.properties.HasRotation;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class CeilingHangingSignData extends BlockData implements HasAttached, HasRotation {
    private static final int ATTACHED_RADIX = 2;

    private static final int ROTATION_RADIX = 16;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = ATTACHED_RADIX * ROTATION_RADIX * WATERLOGGED_RADIX - 1;

    private boolean attached = false;

    private @Range(from = 0, to = 15) int rotation = 0;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public CeilingHangingSignData(int startingID) {
        super(startingID);
    }

    @Override
    public boolean attached() {
        return this.attached;
    }

    @Override
    public void attached(boolean attached) {
        this.attached = attached;
    }

    private int attachedToIndex() {
        return this.attached ? 0 : 1;
    }

    private void attachedFromIndex(int index) {
        this.attached(index == 0);
    }

    private void attachedFromString(String value) {
        this.attached(Boolean.parseBoolean(value));
    }

    @Override
    public @Range(from = 0, to = 15) int rotation() {
        return this.rotation;
    }

    @Override
    public void rotation(@Range(from = 0, to = 15) int rotation) {
        Preconditions.checkArgument(0 <= rotation && rotation <= 15, "Value must be in [0, 15], got " + rotation);
        this.rotation = rotation;
    }

    private int rotationToIndex() {
        return this.rotation;
    }

    private void rotationFromIndex(int index) {
        this.rotation(index);
    }

    private void rotationFromString(String value) {
        this.rotation(Integer.parseInt(value));
    }

    public boolean waterlogged() {
        return this.waterlogged;
    }

    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * ATTACHED_RADIX + attachedToIndex();
        id = id * ROTATION_RADIX + rotationToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        rotationFromIndex(id % ROTATION_RADIX); id /= ROTATION_RADIX;
        attachedFromIndex(id % ATTACHED_RADIX); id /= ATTACHED_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("attached")) {
            attachedFromString(properties.get("attached"));
        }
        if (properties.containsKey("rotation")) {
            rotationFromString(properties.get("rotation"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "CeilingHangingSignData[attached=" + String.valueOf(attached).toLowerCase(Locale.ENGLISH) + ";rotation=" + String.valueOf(rotation).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
