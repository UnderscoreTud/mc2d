package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class SculkShriekerData extends BlockData implements HasWaterlogged {
    private static final int CAN_SUMMON_RADIX = 2;

    private static final int SHRIEKING_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = CAN_SUMMON_RADIX * SHRIEKING_RADIX * WATERLOGGED_RADIX - 1;

    private boolean canSummon = false;

    private boolean shrieking = false;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public SculkShriekerData(int startingID) {
        super(startingID);
    }

    public boolean canSummon() {
        return this.canSummon;
    }

    public void canSummon(boolean canSummon) {
        this.canSummon = canSummon;
    }

    private int canSummonToIndex() {
        return this.canSummon ? 0 : 1;
    }

    private void canSummonFromIndex(int index) {
        this.canSummon(index == 0);
    }

    private void canSummonFromString(String value) {
        this.canSummon(Boolean.parseBoolean(value));
    }

    public boolean shrieking() {
        return this.shrieking;
    }

    public void shrieking(boolean shrieking) {
        this.shrieking = shrieking;
    }

    private int shriekingToIndex() {
        return this.shrieking ? 0 : 1;
    }

    private void shriekingFromIndex(int index) {
        this.shrieking(index == 0);
    }

    private void shriekingFromString(String value) {
        this.shrieking(Boolean.parseBoolean(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * CAN_SUMMON_RADIX + canSummonToIndex();
        id = id * SHRIEKING_RADIX + shriekingToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        shriekingFromIndex(id % SHRIEKING_RADIX); id /= SHRIEKING_RADIX;
        canSummonFromIndex(id % CAN_SUMMON_RADIX); id /= CAN_SUMMON_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("can_summon")) {
            canSummonFromString(properties.get("can_summon"));
        }
        if (properties.containsKey("shrieking")) {
            shriekingFromString(properties.get("shrieking"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "SculkShriekerData[can_summon=" + String.valueOf(canSummon).toLowerCase(Locale.ENGLISH) + ";shrieking=" + String.valueOf(shrieking).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
