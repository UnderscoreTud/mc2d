package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.block.BlockFace;
import me.tud.mc2d.world.blockdata.properties.HasFacing;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class HopperData extends BlockData implements HasFacing {
    private static final int ENABLED_RADIX = 2;

    private static final long FACING_ALLOWED_MASK = (1L << BlockFace.DOWN.ordinal())
            | (1L << BlockFace.NORTH.ordinal())
            | (1L << BlockFace.SOUTH.ordinal())
            | (1L << BlockFace.WEST.ordinal())
            | (1L << BlockFace.EAST.ordinal());

    private static final int FACING_RADIX = 5;

    static final int MAX_OFFSET = ENABLED_RADIX * FACING_RADIX - 1;

    private boolean enabled = true;

    private @BlockFace.Values({BlockFace.DOWN, BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing = BlockFace.DOWN;

    @ApiStatus.Internal
    public HopperData(int startingID) {
        super(startingID);
    }

    public boolean enabled() {
        return this.enabled;
    }

    public void enabled(boolean enabled) {
        this.enabled = enabled;
    }

    private int enabledToIndex() {
        return this.enabled ? 0 : 1;
    }

    private void enabledFromIndex(int index) {
        this.enabled(index == 0);
    }

    private void enabledFromString(String value) {
        this.enabled(Boolean.parseBoolean(value));
    }

    @Override
    public @BlockFace.Values({BlockFace.DOWN, BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing(
            ) {
        return this.facing;
    }

    @Override
    public void facing(
            @BlockFace.Values({BlockFace.DOWN, BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing) {
        Preconditions.checkNotNull(facing, "facing");
        Preconditions.checkArgument((FACING_ALLOWED_MASK & (1L << facing.ordinal())) != 0L, "Invalid facing: " + facing);
        this.facing = facing;
    }

    private int facingToIndex() {
        return switch (this.facing) {
            case DOWN -> 0;
            case NORTH -> 1;
            case SOUTH -> 2;
            case WEST -> 3;
            case EAST -> 4;
            default -> throw new IllegalArgumentException("Invalid facing: " + facing);
        };
    }

    private void facingFromIndex(int index) {
        this.facing(switch (index) {
            case 0 -> BlockFace.DOWN;
            case 1 -> BlockFace.NORTH;
            case 2 -> BlockFace.SOUTH;
            case 3 -> BlockFace.WEST;
            case 4 -> BlockFace.EAST;
            default -> throw new IllegalArgumentException("Index must be between [0, 4], got " + index);
        });
    }

    private void facingFromString(String value) {
        this.facing(BlockFace.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * ENABLED_RADIX + enabledToIndex();
        id = id * FACING_RADIX + facingToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        facingFromIndex(id % FACING_RADIX); id /= FACING_RADIX;
        enabledFromIndex(id % ENABLED_RADIX); id /= ENABLED_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("enabled")) {
            enabledFromString(properties.get("enabled"));
        }
        if (properties.containsKey("facing")) {
            facingFromString(properties.get("facing"));
        }
    }

    @Override
    public String toString() {
        return "HopperData[enabled=" + String.valueOf(enabled).toLowerCase(Locale.ENGLISH) + ";facing=" + String.valueOf(facing).toLowerCase(Locale.ENGLISH) + "]";
    }
}
