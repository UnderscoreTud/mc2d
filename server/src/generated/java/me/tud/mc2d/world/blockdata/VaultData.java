package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.block.BlockFace;
import me.tud.mc2d.world.blockdata.properties.HasFacing;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class VaultData extends BlockData implements HasFacing {
    private static final long FACING_ALLOWED_MASK = (1L << BlockFace.NORTH.ordinal())
            | (1L << BlockFace.SOUTH.ordinal())
            | (1L << BlockFace.WEST.ordinal())
            | (1L << BlockFace.EAST.ordinal());

    private static final int FACING_RADIX = 4;

    private static final int OMINOUS_RADIX = 2;

    private static final int VAULT_STATE_RADIX = 4;

    static final int MAX_OFFSET = FACING_RADIX * OMINOUS_RADIX * VAULT_STATE_RADIX - 1;

    private @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing = BlockFace.NORTH;

    private boolean ominous = false;

    private VaultState vaultState = VaultState.INACTIVE;

    @ApiStatus.Internal
    public VaultData(int startingID) {
        super(startingID);
    }

    @Override
    public @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing(
            ) {
        return this.facing;
    }

    @Override
    public void facing(
            @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing) {
        Preconditions.checkNotNull(facing, "facing");
        Preconditions.checkArgument((FACING_ALLOWED_MASK & (1L << facing.ordinal())) != 0L, "Invalid facing: " + facing);
        this.facing = facing;
    }

    private int facingToIndex() {
        return switch (this.facing) {
            case NORTH -> 0;
            case SOUTH -> 1;
            case WEST -> 2;
            case EAST -> 3;
            default -> throw new IllegalArgumentException("Invalid facing: " + facing);
        };
    }

    private void facingFromIndex(int index) {
        this.facing(switch (index) {
            case 0 -> BlockFace.NORTH;
            case 1 -> BlockFace.SOUTH;
            case 2 -> BlockFace.WEST;
            case 3 -> BlockFace.EAST;
            default -> throw new IllegalArgumentException("Index must be between [0, 3], got " + index);
        });
    }

    private void facingFromString(String value) {
        this.facing(BlockFace.parse(value));
    }

    public boolean ominous() {
        return this.ominous;
    }

    public void ominous(boolean ominous) {
        this.ominous = ominous;
    }

    private int ominousToIndex() {
        return this.ominous ? 0 : 1;
    }

    private void ominousFromIndex(int index) {
        this.ominous(index == 0);
    }

    private void ominousFromString(String value) {
        this.ominous(Boolean.parseBoolean(value));
    }

    public VaultState vaultState() {
        return this.vaultState;
    }

    public void vaultState(VaultState vaultState) {
        Preconditions.checkNotNull(vaultState, "vaultState");
        this.vaultState = vaultState;
    }

    private int vaultStateToIndex() {
        return this.vaultState.ordinal();
    }

    private void vaultStateFromIndex(int index) {
        this.vaultState(VaultState.values()[index]);
    }

    private void vaultStateFromString(String value) {
        this.vaultState(VaultState.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * FACING_RADIX + facingToIndex();
        id = id * OMINOUS_RADIX + ominousToIndex();
        id = id * VAULT_STATE_RADIX + vaultStateToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        vaultStateFromIndex(id % VAULT_STATE_RADIX); id /= VAULT_STATE_RADIX;
        ominousFromIndex(id % OMINOUS_RADIX); id /= OMINOUS_RADIX;
        facingFromIndex(id % FACING_RADIX); id /= FACING_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("facing")) {
            facingFromString(properties.get("facing"));
        }
        if (properties.containsKey("ominous")) {
            ominousFromString(properties.get("ominous"));
        }
        if (properties.containsKey("vault_state")) {
            vaultStateFromString(properties.get("vault_state"));
        }
    }

    @Override
    public String toString() {
        return "VaultData[facing=" + String.valueOf(facing).toLowerCase(Locale.ENGLISH) + ";ominous=" + String.valueOf(ominous).toLowerCase(Locale.ENGLISH) + ";vault_state=" + String.valueOf(vaultState).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum VaultState {
        INACTIVE,

        ACTIVE,

        UNLOCKING,

        EJECTING;

        public static VaultState parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "inactive" -> VaultState.INACTIVE;
                case "active" -> VaultState.ACTIVE;
                case "unlocking" -> VaultState.UNLOCKING;
                case "ejecting" -> VaultState.EJECTING;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
