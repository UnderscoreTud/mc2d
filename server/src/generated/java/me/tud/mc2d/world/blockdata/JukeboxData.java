package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class JukeboxData extends BlockData {
    private static final int HAS_RECORD_RADIX = 2;

    static final int MAX_OFFSET = HAS_RECORD_RADIX - 1;

    private boolean hasRecord = false;

    @ApiStatus.Internal
    public JukeboxData(int startingID) {
        super(startingID);
    }

    public boolean hasRecord() {
        return this.hasRecord;
    }

    public void hasRecord(boolean hasRecord) {
        this.hasRecord = hasRecord;
    }

    private int hasRecordToIndex() {
        return this.hasRecord ? 0 : 1;
    }

    private void hasRecordFromIndex(int index) {
        this.hasRecord(index == 0);
    }

    private void hasRecordFromString(String value) {
        this.hasRecord(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * HAS_RECORD_RADIX + hasRecordToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        hasRecordFromIndex(id % HAS_RECORD_RADIX); id /= HAS_RECORD_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("has_record")) {
            hasRecordFromString(properties.get("has_record"));
        }
    }

    @Override
    public String toString() {
        return "JukeboxData[has_record=" + String.valueOf(hasRecord).toLowerCase(Locale.ENGLISH) + "]";
    }
}
