package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class SculkCatalystData extends BlockData {
    private static final int BLOOM_RADIX = 2;

    static final int MAX_OFFSET = BLOOM_RADIX - 1;

    private boolean bloom = false;

    @ApiStatus.Internal
    public SculkCatalystData(int startingID) {
        super(startingID);
    }

    public boolean bloom() {
        return this.bloom;
    }

    public void bloom(boolean bloom) {
        this.bloom = bloom;
    }

    private int bloomToIndex() {
        return this.bloom ? 0 : 1;
    }

    private void bloomFromIndex(int index) {
        this.bloom(index == 0);
    }

    private void bloomFromString(String value) {
        this.bloom(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * BLOOM_RADIX + bloomToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        bloomFromIndex(id % BLOOM_RADIX); id /= BLOOM_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("bloom")) {
            bloomFromString(properties.get("bloom"));
        }
    }

    @Override
    public String toString() {
        return "SculkCatalystData[bloom=" + String.valueOf(bloom).toLowerCase(Locale.ENGLISH) + "]";
    }
}
