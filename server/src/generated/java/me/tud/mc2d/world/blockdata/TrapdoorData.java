package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.block.BlockFace;
import me.tud.mc2d.world.blockdata.properties.HasFacing;
import me.tud.mc2d.world.blockdata.properties.HasOpen;
import me.tud.mc2d.world.blockdata.properties.HasPowered;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class TrapdoorData extends BlockData implements HasFacing,
        HasOpen,
        HasPowered,
        HasWaterlogged {
    private static final long FACING_ALLOWED_MASK = (1L << BlockFace.NORTH.ordinal())
            | (1L << BlockFace.SOUTH.ordinal())
            | (1L << BlockFace.WEST.ordinal())
            | (1L << BlockFace.EAST.ordinal());

    private static final int FACING_RADIX = 4;

    private static final int HALF_RADIX = 2;

    private static final int OPEN_RADIX = 2;

    private static final int POWERED_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = FACING_RADIX * HALF_RADIX * OPEN_RADIX * POWERED_RADIX * WATERLOGGED_RADIX - 1;

    private @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing = BlockFace.NORTH;

    private Half half = Half.BOTTOM;

    private boolean open = false;

    private boolean powered = false;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public TrapdoorData(int startingID) {
        super(startingID);
    }

    @Override
    public @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing(
            ) {
        return this.facing;
    }

    @Override
    public void facing(
            @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing) {
        Preconditions.checkNotNull(facing, "facing");
        Preconditions.checkArgument((FACING_ALLOWED_MASK & (1L << facing.ordinal())) != 0L, "Invalid facing: " + facing);
        this.facing = facing;
    }

    private int facingToIndex() {
        return switch (this.facing) {
            case NORTH -> 0;
            case SOUTH -> 1;
            case WEST -> 2;
            case EAST -> 3;
            default -> throw new IllegalArgumentException("Invalid facing: " + facing);
        };
    }

    private void facingFromIndex(int index) {
        this.facing(switch (index) {
            case 0 -> BlockFace.NORTH;
            case 1 -> BlockFace.SOUTH;
            case 2 -> BlockFace.WEST;
            case 3 -> BlockFace.EAST;
            default -> throw new IllegalArgumentException("Index must be between [0, 3], got " + index);
        });
    }

    private void facingFromString(String value) {
        this.facing(BlockFace.parse(value));
    }

    public Half half() {
        return this.half;
    }

    public void half(Half half) {
        Preconditions.checkNotNull(half, "half");
        this.half = half;
    }

    private int halfToIndex() {
        return this.half.ordinal();
    }

    private void halfFromIndex(int index) {
        this.half(Half.values()[index]);
    }

    private void halfFromString(String value) {
        this.half(Half.parse(value));
    }

    @Override
    public boolean open() {
        return this.open;
    }

    @Override
    public void open(boolean open) {
        this.open = open;
    }

    private int openToIndex() {
        return this.open ? 0 : 1;
    }

    private void openFromIndex(int index) {
        this.open(index == 0);
    }

    private void openFromString(String value) {
        this.open(Boolean.parseBoolean(value));
    }

    @Override
    public boolean powered() {
        return this.powered;
    }

    @Override
    public void powered(boolean powered) {
        this.powered = powered;
    }

    private int poweredToIndex() {
        return this.powered ? 0 : 1;
    }

    private void poweredFromIndex(int index) {
        this.powered(index == 0);
    }

    private void poweredFromString(String value) {
        this.powered(Boolean.parseBoolean(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * FACING_RADIX + facingToIndex();
        id = id * HALF_RADIX + halfToIndex();
        id = id * OPEN_RADIX + openToIndex();
        id = id * POWERED_RADIX + poweredToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        poweredFromIndex(id % POWERED_RADIX); id /= POWERED_RADIX;
        openFromIndex(id % OPEN_RADIX); id /= OPEN_RADIX;
        halfFromIndex(id % HALF_RADIX); id /= HALF_RADIX;
        facingFromIndex(id % FACING_RADIX); id /= FACING_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("facing")) {
            facingFromString(properties.get("facing"));
        }
        if (properties.containsKey("half")) {
            halfFromString(properties.get("half"));
        }
        if (properties.containsKey("open")) {
            openFromString(properties.get("open"));
        }
        if (properties.containsKey("powered")) {
            poweredFromString(properties.get("powered"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "TrapdoorData[facing=" + String.valueOf(facing).toLowerCase(Locale.ENGLISH) + ";half=" + String.valueOf(half).toLowerCase(Locale.ENGLISH) + ";open=" + String.valueOf(open).toLowerCase(Locale.ENGLISH) + ";powered=" + String.valueOf(powered).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Half {
        TOP,

        BOTTOM;

        public static Half parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "top" -> Half.TOP;
                case "bottom" -> Half.BOTTOM;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
