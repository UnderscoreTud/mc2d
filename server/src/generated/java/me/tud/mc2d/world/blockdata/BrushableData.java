package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class BrushableData extends BlockData {
    private static final int DUSTED_RADIX = 4;

    static final int MAX_OFFSET = DUSTED_RADIX - 1;

    private @Range(from = 0, to = 3) int dusted = 0;

    @ApiStatus.Internal
    public BrushableData(int startingID) {
        super(startingID);
    }

    public @Range(from = 0, to = 3) int dusted() {
        return this.dusted;
    }

    public void dusted(@Range(from = 0, to = 3) int dusted) {
        Preconditions.checkArgument(0 <= dusted && dusted <= 3, "Value must be in [0, 3], got " + dusted);
        this.dusted = dusted;
    }

    private int dustedToIndex() {
        return this.dusted;
    }

    private void dustedFromIndex(int index) {
        this.dusted(index);
    }

    private void dustedFromString(String value) {
        this.dusted(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * DUSTED_RADIX + dustedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        dustedFromIndex(id % DUSTED_RADIX); id /= DUSTED_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("dusted")) {
            dustedFromString(properties.get("dusted"));
        }
    }

    @Override
    public String toString() {
        return "BrushableData[dusted=" + String.valueOf(dusted).toLowerCase(Locale.ENGLISH) + "]";
    }
}
