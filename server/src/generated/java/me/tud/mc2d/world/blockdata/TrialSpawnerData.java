package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasOminous;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class TrialSpawnerData extends BlockData implements HasOminous {
    private static final int OMINOUS_RADIX = 2;

    private static final int TRIAL_SPAWNER_STATE_RADIX = 6;

    static final int MAX_OFFSET = OMINOUS_RADIX * TRIAL_SPAWNER_STATE_RADIX - 1;

    private boolean ominous = false;

    private TrialSpawnerState trialSpawnerState = TrialSpawnerState.INACTIVE;

    @ApiStatus.Internal
    public TrialSpawnerData(int startingID) {
        super(startingID);
    }

    @Override
    public boolean ominous() {
        return this.ominous;
    }

    @Override
    public void ominous(boolean ominous) {
        this.ominous = ominous;
    }

    private int ominousToIndex() {
        return this.ominous ? 0 : 1;
    }

    private void ominousFromIndex(int index) {
        this.ominous(index == 0);
    }

    private void ominousFromString(String value) {
        this.ominous(Boolean.parseBoolean(value));
    }

    public TrialSpawnerState trialSpawnerState() {
        return this.trialSpawnerState;
    }

    public void trialSpawnerState(TrialSpawnerState trialSpawnerState) {
        Preconditions.checkNotNull(trialSpawnerState, "trialSpawnerState");
        this.trialSpawnerState = trialSpawnerState;
    }

    private int trialSpawnerStateToIndex() {
        return this.trialSpawnerState.ordinal();
    }

    private void trialSpawnerStateFromIndex(int index) {
        this.trialSpawnerState(TrialSpawnerState.values()[index]);
    }

    private void trialSpawnerStateFromString(String value) {
        this.trialSpawnerState(TrialSpawnerState.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * OMINOUS_RADIX + ominousToIndex();
        id = id * TRIAL_SPAWNER_STATE_RADIX + trialSpawnerStateToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        trialSpawnerStateFromIndex(id % TRIAL_SPAWNER_STATE_RADIX); id /= TRIAL_SPAWNER_STATE_RADIX;
        ominousFromIndex(id % OMINOUS_RADIX); id /= OMINOUS_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("ominous")) {
            ominousFromString(properties.get("ominous"));
        }
        if (properties.containsKey("trial_spawner_state")) {
            trialSpawnerStateFromString(properties.get("trial_spawner_state"));
        }
    }

    @Override
    public String toString() {
        return "TrialSpawnerData[ominous=" + String.valueOf(ominous).toLowerCase(Locale.ENGLISH) + ";trial_spawner_state=" + String.valueOf(trialSpawnerState).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum TrialSpawnerState {
        INACTIVE,

        WAITING_FOR_PLAYERS,

        ACTIVE,

        WAITING_FOR_REWARD_EJECTION,

        EJECTING_REWARD,

        COOLDOWN;

        public static TrialSpawnerState parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "inactive" -> TrialSpawnerState.INACTIVE;
                case "waiting_for_players" -> TrialSpawnerState.WAITING_FOR_PLAYERS;
                case "active" -> TrialSpawnerState.ACTIVE;
                case "waiting_for_reward_ejection" -> TrialSpawnerState.WAITING_FOR_REWARD_EJECTION;
                case "ejecting_reward" -> TrialSpawnerState.EJECTING_REWARD;
                case "cooldown" -> TrialSpawnerState.COOLDOWN;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
