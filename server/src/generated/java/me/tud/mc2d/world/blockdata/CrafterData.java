package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasOrientation;
import me.tud.mc2d.world.blockdata.properties.HasTriggered;
import me.tud.mc2d.world.blockdata.properties.OrientationProperty;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class CrafterData extends BlockData implements HasOrientation, HasTriggered {
    private static final int CRAFTING_RADIX = 2;

    private static final int ORIENTATION_RADIX = 12;

    private static final int TRIGGERED_RADIX = 2;

    static final int MAX_OFFSET = CRAFTING_RADIX * ORIENTATION_RADIX * TRIGGERED_RADIX - 1;

    private boolean crafting = false;

    private OrientationProperty orientation = OrientationProperty.NORTH_UP;

    private boolean triggered = false;

    @ApiStatus.Internal
    public CrafterData(int startingID) {
        super(startingID);
    }

    public boolean crafting() {
        return this.crafting;
    }

    public void crafting(boolean crafting) {
        this.crafting = crafting;
    }

    private int craftingToIndex() {
        return this.crafting ? 0 : 1;
    }

    private void craftingFromIndex(int index) {
        this.crafting(index == 0);
    }

    private void craftingFromString(String value) {
        this.crafting(Boolean.parseBoolean(value));
    }

    @Override
    public OrientationProperty orientation() {
        return this.orientation;
    }

    @Override
    public void orientation(OrientationProperty orientation) {
        Preconditions.checkNotNull(orientation, "orientation");
        this.orientation = orientation;
    }

    private int orientationToIndex() {
        return this.orientation.ordinal();
    }

    private void orientationFromIndex(int index) {
        this.orientation(OrientationProperty.values()[index]);
    }

    private void orientationFromString(String value) {
        this.orientation(OrientationProperty.parse(value));
    }

    @Override
    public boolean triggered() {
        return this.triggered;
    }

    @Override
    public void triggered(boolean triggered) {
        this.triggered = triggered;
    }

    private int triggeredToIndex() {
        return this.triggered ? 0 : 1;
    }

    private void triggeredFromIndex(int index) {
        this.triggered(index == 0);
    }

    private void triggeredFromString(String value) {
        this.triggered(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * CRAFTING_RADIX + craftingToIndex();
        id = id * ORIENTATION_RADIX + orientationToIndex();
        id = id * TRIGGERED_RADIX + triggeredToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        triggeredFromIndex(id % TRIGGERED_RADIX); id /= TRIGGERED_RADIX;
        orientationFromIndex(id % ORIENTATION_RADIX); id /= ORIENTATION_RADIX;
        craftingFromIndex(id % CRAFTING_RADIX); id /= CRAFTING_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("crafting")) {
            craftingFromString(properties.get("crafting"));
        }
        if (properties.containsKey("orientation")) {
            orientationFromString(properties.get("orientation"));
        }
        if (properties.containsKey("triggered")) {
            triggeredFromString(properties.get("triggered"));
        }
    }

    @Override
    public String toString() {
        return "CrafterData[crafting=" + String.valueOf(crafting).toLowerCase(Locale.ENGLISH) + ";orientation=" + String.valueOf(orientation).toLowerCase(Locale.ENGLISH) + ";triggered=" + String.valueOf(triggered).toLowerCase(Locale.ENGLISH) + "]";
    }
}
