package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class PointedDripstoneData extends BlockData implements HasWaterlogged {
    private static final int THICKNESS_RADIX = 5;

    private static final int VERTICAL_DIRECTION_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = THICKNESS_RADIX * VERTICAL_DIRECTION_RADIX * WATERLOGGED_RADIX - 1;

    private Thickness thickness = Thickness.TIP;

    private VerticalDirection verticalDirection = VerticalDirection.UP;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public PointedDripstoneData(int startingID) {
        super(startingID);
    }

    public Thickness thickness() {
        return this.thickness;
    }

    public void thickness(Thickness thickness) {
        Preconditions.checkNotNull(thickness, "thickness");
        this.thickness = thickness;
    }

    private int thicknessToIndex() {
        return this.thickness.ordinal();
    }

    private void thicknessFromIndex(int index) {
        this.thickness(Thickness.values()[index]);
    }

    private void thicknessFromString(String value) {
        this.thickness(Thickness.parse(value));
    }

    public VerticalDirection verticalDirection() {
        return this.verticalDirection;
    }

    public void verticalDirection(VerticalDirection verticalDirection) {
        Preconditions.checkNotNull(verticalDirection, "verticalDirection");
        this.verticalDirection = verticalDirection;
    }

    private int verticalDirectionToIndex() {
        return this.verticalDirection.ordinal();
    }

    private void verticalDirectionFromIndex(int index) {
        this.verticalDirection(VerticalDirection.values()[index]);
    }

    private void verticalDirectionFromString(String value) {
        this.verticalDirection(VerticalDirection.parse(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * THICKNESS_RADIX + thicknessToIndex();
        id = id * VERTICAL_DIRECTION_RADIX + verticalDirectionToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        verticalDirectionFromIndex(id % VERTICAL_DIRECTION_RADIX); id /= VERTICAL_DIRECTION_RADIX;
        thicknessFromIndex(id % THICKNESS_RADIX); id /= THICKNESS_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("thickness")) {
            thicknessFromString(properties.get("thickness"));
        }
        if (properties.containsKey("vertical_direction")) {
            verticalDirectionFromString(properties.get("vertical_direction"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "PointedDripstoneData[thickness=" + String.valueOf(thickness).toLowerCase(Locale.ENGLISH) + ";vertical_direction=" + String.valueOf(verticalDirection).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Thickness {
        TIP_MERGE,

        TIP,

        FRUSTUM,

        MIDDLE,

        BASE;

        public static Thickness parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "tip_merge" -> Thickness.TIP_MERGE;
                case "tip" -> Thickness.TIP;
                case "frustum" -> Thickness.FRUSTUM;
                case "middle" -> Thickness.MIDDLE;
                case "base" -> Thickness.BASE;
                default -> throw new IllegalArgumentException();
            };
        }
    }

    public enum VerticalDirection {
        UP,

        DOWN;

        public static VerticalDirection parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "up" -> VerticalDirection.UP;
                case "down" -> VerticalDirection.DOWN;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
