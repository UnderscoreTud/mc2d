package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.block.BlockFace;
import me.tud.mc2d.world.blockdata.properties.HasFacing;
import me.tud.mc2d.world.blockdata.properties.HasPower;
import me.tud.mc2d.world.blockdata.properties.HasSculkSensorPhase;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import me.tud.mc2d.world.blockdata.properties.SculkSensorPhaseProperty;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class CalibratedSculkSensorData extends BlockData implements HasFacing,
        HasPower,
        HasSculkSensorPhase,
        HasWaterlogged {
    private static final long FACING_ALLOWED_MASK = (1L << BlockFace.NORTH.ordinal())
            | (1L << BlockFace.SOUTH.ordinal())
            | (1L << BlockFace.WEST.ordinal())
            | (1L << BlockFace.EAST.ordinal());

    private static final int FACING_RADIX = 4;

    private static final int POWER_RADIX = 16;

    private static final int SCULK_SENSOR_PHASE_RADIX = 3;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = FACING_RADIX * POWER_RADIX * SCULK_SENSOR_PHASE_RADIX * WATERLOGGED_RADIX - 1;

    private @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing = BlockFace.NORTH;

    private @Range(from = 0, to = 15) int power = 0;

    private SculkSensorPhaseProperty sculkSensorPhase = SculkSensorPhaseProperty.INACTIVE;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public CalibratedSculkSensorData(int startingID) {
        super(startingID);
    }

    @Override
    public @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing(
            ) {
        return this.facing;
    }

    @Override
    public void facing(
            @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing) {
        Preconditions.checkNotNull(facing, "facing");
        Preconditions.checkArgument((FACING_ALLOWED_MASK & (1L << facing.ordinal())) != 0L, "Invalid facing: " + facing);
        this.facing = facing;
    }

    private int facingToIndex() {
        return switch (this.facing) {
            case NORTH -> 0;
            case SOUTH -> 1;
            case WEST -> 2;
            case EAST -> 3;
            default -> throw new IllegalArgumentException("Invalid facing: " + facing);
        };
    }

    private void facingFromIndex(int index) {
        this.facing(switch (index) {
            case 0 -> BlockFace.NORTH;
            case 1 -> BlockFace.SOUTH;
            case 2 -> BlockFace.WEST;
            case 3 -> BlockFace.EAST;
            default -> throw new IllegalArgumentException("Index must be between [0, 3], got " + index);
        });
    }

    private void facingFromString(String value) {
        this.facing(BlockFace.parse(value));
    }

    @Override
    public @Range(from = 0, to = 15) int power() {
        return this.power;
    }

    @Override
    public void power(@Range(from = 0, to = 15) int power) {
        Preconditions.checkArgument(0 <= power && power <= 15, "Value must be in [0, 15], got " + power);
        this.power = power;
    }

    private int powerToIndex() {
        return this.power;
    }

    private void powerFromIndex(int index) {
        this.power(index);
    }

    private void powerFromString(String value) {
        this.power(Integer.parseInt(value));
    }

    @Override
    public SculkSensorPhaseProperty sculkSensorPhase() {
        return this.sculkSensorPhase;
    }

    @Override
    public void sculkSensorPhase(SculkSensorPhaseProperty sculkSensorPhase) {
        Preconditions.checkNotNull(sculkSensorPhase, "sculkSensorPhase");
        this.sculkSensorPhase = sculkSensorPhase;
    }

    private int sculkSensorPhaseToIndex() {
        return this.sculkSensorPhase.ordinal();
    }

    private void sculkSensorPhaseFromIndex(int index) {
        this.sculkSensorPhase(SculkSensorPhaseProperty.values()[index]);
    }

    private void sculkSensorPhaseFromString(String value) {
        this.sculkSensorPhase(SculkSensorPhaseProperty.parse(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * FACING_RADIX + facingToIndex();
        id = id * POWER_RADIX + powerToIndex();
        id = id * SCULK_SENSOR_PHASE_RADIX + sculkSensorPhaseToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        sculkSensorPhaseFromIndex(id % SCULK_SENSOR_PHASE_RADIX); id /= SCULK_SENSOR_PHASE_RADIX;
        powerFromIndex(id % POWER_RADIX); id /= POWER_RADIX;
        facingFromIndex(id % FACING_RADIX); id /= FACING_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("facing")) {
            facingFromString(properties.get("facing"));
        }
        if (properties.containsKey("power")) {
            powerFromString(properties.get("power"));
        }
        if (properties.containsKey("sculk_sensor_phase")) {
            sculkSensorPhaseFromString(properties.get("sculk_sensor_phase"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "CalibratedSculkSensorData[facing=" + String.valueOf(facing).toLowerCase(Locale.ENGLISH) + ";power=" + String.valueOf(power).toLowerCase(Locale.ENGLISH) + ";sculk_sensor_phase=" + String.valueOf(sculkSensorPhase).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
