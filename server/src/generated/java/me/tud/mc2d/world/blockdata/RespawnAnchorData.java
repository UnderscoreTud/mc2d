package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class RespawnAnchorData extends BlockData {
    private static final int CHARGES_RADIX = 5;

    static final int MAX_OFFSET = CHARGES_RADIX - 1;

    private @Range(from = 0, to = 4) int charges = 0;

    @ApiStatus.Internal
    public RespawnAnchorData(int startingID) {
        super(startingID);
    }

    public @Range(from = 0, to = 4) int charges() {
        return this.charges;
    }

    public void charges(@Range(from = 0, to = 4) int charges) {
        Preconditions.checkArgument(0 <= charges && charges <= 4, "Value must be in [0, 4], got " + charges);
        this.charges = charges;
    }

    private int chargesToIndex() {
        return this.charges;
    }

    private void chargesFromIndex(int index) {
        this.charges(index);
    }

    private void chargesFromString(String value) {
        this.charges(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * CHARGES_RADIX + chargesToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        chargesFromIndex(id % CHARGES_RADIX); id /= CHARGES_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("charges")) {
            chargesFromString(properties.get("charges"));
        }
    }

    @Override
    public String toString() {
        return "RespawnAnchorData[charges=" + String.valueOf(charges).toLowerCase(Locale.ENGLISH) + "]";
    }
}
