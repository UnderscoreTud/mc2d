package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class BubbleColumnData extends BlockData {
    private static final int DRAG_RADIX = 2;

    static final int MAX_OFFSET = DRAG_RADIX - 1;

    private boolean drag = true;

    @ApiStatus.Internal
    public BubbleColumnData(int startingID) {
        super(startingID);
    }

    public boolean drag() {
        return this.drag;
    }

    public void drag(boolean drag) {
        this.drag = drag;
    }

    private int dragToIndex() {
        return this.drag ? 0 : 1;
    }

    private void dragFromIndex(int index) {
        this.drag(index == 0);
    }

    private void dragFromString(String value) {
        this.drag(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * DRAG_RADIX + dragToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        dragFromIndex(id % DRAG_RADIX); id /= DRAG_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("drag")) {
            dragFromString(properties.get("drag"));
        }
    }

    @Override
    public String toString() {
        return "BubbleColumnData[drag=" + String.valueOf(drag).toLowerCase(Locale.ENGLISH) + "]";
    }
}
