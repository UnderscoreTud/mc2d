package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.block.BlockFace;
import me.tud.mc2d.world.blockdata.properties.HasFacing;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class BedData extends BlockData implements HasFacing {
    private static final long FACING_ALLOWED_MASK = (1L << BlockFace.NORTH.ordinal())
            | (1L << BlockFace.SOUTH.ordinal())
            | (1L << BlockFace.WEST.ordinal())
            | (1L << BlockFace.EAST.ordinal());

    private static final int FACING_RADIX = 4;

    private static final int OCCUPIED_RADIX = 2;

    private static final int PART_RADIX = 2;

    static final int MAX_OFFSET = FACING_RADIX * OCCUPIED_RADIX * PART_RADIX - 1;

    private @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing = BlockFace.NORTH;

    private boolean occupied = false;

    private Part part = Part.FOOT;

    @ApiStatus.Internal
    public BedData(int startingID) {
        super(startingID);
    }

    @Override
    public @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing(
            ) {
        return this.facing;
    }

    @Override
    public void facing(
            @BlockFace.Values({BlockFace.NORTH, BlockFace.SOUTH, BlockFace.WEST, BlockFace.EAST}) BlockFace facing) {
        Preconditions.checkNotNull(facing, "facing");
        Preconditions.checkArgument((FACING_ALLOWED_MASK & (1L << facing.ordinal())) != 0L, "Invalid facing: " + facing);
        this.facing = facing;
    }

    private int facingToIndex() {
        return switch (this.facing) {
            case NORTH -> 0;
            case SOUTH -> 1;
            case WEST -> 2;
            case EAST -> 3;
            default -> throw new IllegalArgumentException("Invalid facing: " + facing);
        };
    }

    private void facingFromIndex(int index) {
        this.facing(switch (index) {
            case 0 -> BlockFace.NORTH;
            case 1 -> BlockFace.SOUTH;
            case 2 -> BlockFace.WEST;
            case 3 -> BlockFace.EAST;
            default -> throw new IllegalArgumentException("Index must be between [0, 3], got " + index);
        });
    }

    private void facingFromString(String value) {
        this.facing(BlockFace.parse(value));
    }

    public boolean occupied() {
        return this.occupied;
    }

    public void occupied(boolean occupied) {
        this.occupied = occupied;
    }

    private int occupiedToIndex() {
        return this.occupied ? 0 : 1;
    }

    private void occupiedFromIndex(int index) {
        this.occupied(index == 0);
    }

    private void occupiedFromString(String value) {
        this.occupied(Boolean.parseBoolean(value));
    }

    public Part part() {
        return this.part;
    }

    public void part(Part part) {
        Preconditions.checkNotNull(part, "part");
        this.part = part;
    }

    private int partToIndex() {
        return this.part.ordinal();
    }

    private void partFromIndex(int index) {
        this.part(Part.values()[index]);
    }

    private void partFromString(String value) {
        this.part(Part.parse(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * FACING_RADIX + facingToIndex();
        id = id * OCCUPIED_RADIX + occupiedToIndex();
        id = id * PART_RADIX + partToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        partFromIndex(id % PART_RADIX); id /= PART_RADIX;
        occupiedFromIndex(id % OCCUPIED_RADIX); id /= OCCUPIED_RADIX;
        facingFromIndex(id % FACING_RADIX); id /= FACING_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("facing")) {
            facingFromString(properties.get("facing"));
        }
        if (properties.containsKey("occupied")) {
            occupiedFromString(properties.get("occupied"));
        }
        if (properties.containsKey("part")) {
            partFromString(properties.get("part"));
        }
    }

    @Override
    public String toString() {
        return "BedData[facing=" + String.valueOf(facing).toLowerCase(Locale.ENGLISH) + ";occupied=" + String.valueOf(occupied).toLowerCase(Locale.ENGLISH) + ";part=" + String.valueOf(part).toLowerCase(Locale.ENGLISH) + "]";
    }

    public enum Part {
        HEAD,

        FOOT;

        public static Part parse(String value) {
            return switch (value.toLowerCase(Locale.ENGLISH)) {
                case "head" -> Part.HEAD;
                case "foot" -> Part.FOOT;
                default -> throw new IllegalArgumentException();
            };
        }
    }
}
