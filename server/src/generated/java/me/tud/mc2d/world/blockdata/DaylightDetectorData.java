package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class DaylightDetectorData extends BlockData {
    private static final int INVERTED_RADIX = 2;

    private static final int POWER_RADIX = 16;

    static final int MAX_OFFSET = INVERTED_RADIX * POWER_RADIX - 1;

    private boolean inverted = false;

    private @Range(from = 0, to = 15) int power = 0;

    @ApiStatus.Internal
    public DaylightDetectorData(int startingID) {
        super(startingID);
    }

    public boolean inverted() {
        return this.inverted;
    }

    public void inverted(boolean inverted) {
        this.inverted = inverted;
    }

    private int invertedToIndex() {
        return this.inverted ? 0 : 1;
    }

    private void invertedFromIndex(int index) {
        this.inverted(index == 0);
    }

    private void invertedFromString(String value) {
        this.inverted(Boolean.parseBoolean(value));
    }

    public @Range(from = 0, to = 15) int power() {
        return this.power;
    }

    public void power(@Range(from = 0, to = 15) int power) {
        Preconditions.checkArgument(0 <= power && power <= 15, "Value must be in [0, 15], got " + power);
        this.power = power;
    }

    private int powerToIndex() {
        return this.power;
    }

    private void powerFromIndex(int index) {
        this.power(index);
    }

    private void powerFromString(String value) {
        this.power(Integer.parseInt(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * INVERTED_RADIX + invertedToIndex();
        id = id * POWER_RADIX + powerToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        powerFromIndex(id % POWER_RADIX); id /= POWER_RADIX;
        invertedFromIndex(id % INVERTED_RADIX); id /= INVERTED_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("inverted")) {
            invertedFromString(properties.get("inverted"));
        }
        if (properties.containsKey("power")) {
            powerFromString(properties.get("power"));
        }
    }

    @Override
    public String toString() {
        return "DaylightDetectorData[inverted=" + String.valueOf(inverted).toLowerCase(Locale.ENGLISH) + ";power=" + String.valueOf(power).toLowerCase(Locale.ENGLISH) + "]";
    }
}
