package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasAge;
import me.tud.mc2d.world.blockdata.properties.HasStage;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class MangrovePropaguleData extends BlockData implements HasAge, HasStage, HasWaterlogged {
    private static final int AGE_RADIX = 5;

    private static final int HANGING_RADIX = 2;

    private static final int STAGE_RADIX = 2;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = AGE_RADIX * HANGING_RADIX * STAGE_RADIX * WATERLOGGED_RADIX - 1;

    private @Range(from = 0, to = 4) int age = 0;

    private boolean hanging = false;

    private @Range(from = 0, to = 1) int stage = 0;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public MangrovePropaguleData(int startingID) {
        super(startingID);
    }

    @Override
    public @Range(from = 0, to = 4) int age() {
        return this.age;
    }

    @Override
    public void age(@Range(from = 0, to = 4) int age) {
        Preconditions.checkArgument(0 <= age && age <= 4, "Value must be in [0, 4], got " + age);
        this.age = age;
    }

    private int ageToIndex() {
        return this.age;
    }

    private void ageFromIndex(int index) {
        this.age(index);
    }

    private void ageFromString(String value) {
        this.age(Integer.parseInt(value));
    }

    public boolean hanging() {
        return this.hanging;
    }

    public void hanging(boolean hanging) {
        this.hanging = hanging;
    }

    private int hangingToIndex() {
        return this.hanging ? 0 : 1;
    }

    private void hangingFromIndex(int index) {
        this.hanging(index == 0);
    }

    private void hangingFromString(String value) {
        this.hanging(Boolean.parseBoolean(value));
    }

    @Override
    public @Range(from = 0, to = 1) int stage() {
        return this.stage;
    }

    @Override
    public void stage(@Range(from = 0, to = 1) int stage) {
        Preconditions.checkArgument(0 <= stage && stage <= 1, "Value must be in [0, 1], got " + stage);
        this.stage = stage;
    }

    private int stageToIndex() {
        return this.stage;
    }

    private void stageFromIndex(int index) {
        this.stage(index);
    }

    private void stageFromString(String value) {
        this.stage(Integer.parseInt(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * AGE_RADIX + ageToIndex();
        id = id * HANGING_RADIX + hangingToIndex();
        id = id * STAGE_RADIX + stageToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        stageFromIndex(id % STAGE_RADIX); id /= STAGE_RADIX;
        hangingFromIndex(id % HANGING_RADIX); id /= HANGING_RADIX;
        ageFromIndex(id % AGE_RADIX); id /= AGE_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("age")) {
            ageFromString(properties.get("age"));
        }
        if (properties.containsKey("hanging")) {
            hangingFromString(properties.get("hanging"));
        }
        if (properties.containsKey("stage")) {
            stageFromString(properties.get("stage"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "MangrovePropaguleData[age=" + String.valueOf(age).toLowerCase(Locale.ENGLISH) + ";hanging=" + String.valueOf(hanging).toLowerCase(Locale.ENGLISH) + ";stage=" + String.valueOf(stage).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
