package me.tud.mc2d.world.blockdata;

import com.google.common.base.Preconditions;
import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasDistance;
import me.tud.mc2d.world.blockdata.properties.HasWaterlogged;
import org.jetbrains.annotations.ApiStatus;
import org.jetbrains.annotations.Range;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blockdata.BlockDataGenerator")
public final class ScaffoldingData extends BlockData implements HasDistance, HasWaterlogged {
    private static final int BOTTOM_RADIX = 2;

    private static final int DISTANCE_RADIX = 8;

    private static final int WATERLOGGED_RADIX = 2;

    static final int MAX_OFFSET = BOTTOM_RADIX * DISTANCE_RADIX * WATERLOGGED_RADIX - 1;

    private boolean bottom = false;

    private @Range(from = 0, to = 7) int distance = 7;

    private boolean waterlogged = false;

    @ApiStatus.Internal
    public ScaffoldingData(int startingID) {
        super(startingID);
    }

    public boolean bottom() {
        return this.bottom;
    }

    public void bottom(boolean bottom) {
        this.bottom = bottom;
    }

    private int bottomToIndex() {
        return this.bottom ? 0 : 1;
    }

    private void bottomFromIndex(int index) {
        this.bottom(index == 0);
    }

    private void bottomFromString(String value) {
        this.bottom(Boolean.parseBoolean(value));
    }

    @Override
    public @Range(from = 0, to = 7) int distance() {
        return this.distance;
    }

    @Override
    public void distance(@Range(from = 0, to = 7) int distance) {
        Preconditions.checkArgument(0 <= distance && distance <= 7, "Value must be in [0, 7], got " + distance);
        this.distance = distance;
    }

    private int distanceToIndex() {
        return this.distance;
    }

    private void distanceFromIndex(int index) {
        this.distance(index);
    }

    private void distanceFromString(String value) {
        this.distance(Integer.parseInt(value));
    }

    @Override
    public boolean waterlogged() {
        return this.waterlogged;
    }

    @Override
    public void waterlogged(boolean waterlogged) {
        this.waterlogged = waterlogged;
    }

    private int waterloggedToIndex() {
        return this.waterlogged ? 0 : 1;
    }

    private void waterloggedFromIndex(int index) {
        this.waterlogged(index == 0);
    }

    private void waterloggedFromString(String value) {
        this.waterlogged(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * BOTTOM_RADIX + bottomToIndex();
        id = id * DISTANCE_RADIX + distanceToIndex();
        id = id * WATERLOGGED_RADIX + waterloggedToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        waterloggedFromIndex(id % WATERLOGGED_RADIX); id /= WATERLOGGED_RADIX;
        distanceFromIndex(id % DISTANCE_RADIX); id /= DISTANCE_RADIX;
        bottomFromIndex(id % BOTTOM_RADIX); id /= BOTTOM_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("bottom")) {
            bottomFromString(properties.get("bottom"));
        }
        if (properties.containsKey("distance")) {
            distanceFromString(properties.get("distance"));
        }
        if (properties.containsKey("waterlogged")) {
            waterloggedFromString(properties.get("waterlogged"));
        }
    }

    @Override
    public String toString() {
        return "ScaffoldingData[bottom=" + String.valueOf(bottom).toLowerCase(Locale.ENGLISH) + ";distance=" + String.valueOf(distance).toLowerCase(Locale.ENGLISH) + ";waterlogged=" + String.valueOf(waterlogged).toLowerCase(Locale.ENGLISH) + "]";
    }
}
