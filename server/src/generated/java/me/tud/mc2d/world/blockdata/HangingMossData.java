package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class HangingMossData extends BlockData {
    private static final int TIP_RADIX = 2;

    static final int MAX_OFFSET = TIP_RADIX - 1;

    private boolean tip = true;

    @ApiStatus.Internal
    public HangingMossData(int startingID) {
        super(startingID);
    }

    public boolean tip() {
        return this.tip;
    }

    public void tip(boolean tip) {
        this.tip = tip;
    }

    private int tipToIndex() {
        return this.tip ? 0 : 1;
    }

    private void tipFromIndex(int index) {
        this.tip(index == 0);
    }

    private void tipFromString(String value) {
        this.tip(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * TIP_RADIX + tipToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        tipFromIndex(id % TIP_RADIX); id /= TIP_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("tip")) {
            tipFromString(properties.get("tip"));
        }
    }

    @Override
    public String toString() {
        return "HangingMossData[tip=" + String.valueOf(tip).toLowerCase(Locale.ENGLISH) + "]";
    }
}
