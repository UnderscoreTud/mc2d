package me.tud.mc2d.world.blockdata;

import java.util.Locale;
import java.util.Map;
import javax.annotation.processing.Generated;
import me.tud.mc2d.world.blockdata.properties.HasLit;
import me.tud.mc2d.world.blockdata.properties.HasPowered;
import org.jetbrains.annotations.ApiStatus;

/**
 * This class contains autogenerated code. Please do <b>NOT</b> edit manually.
 */
@Generated("me.tud.mc2d.generators.blocks.BlocksGenerator")
public final class CopperBulbBlockData extends BlockData implements HasLit, HasPowered {
    private static final int LIT_RADIX = 2;

    private static final int POWERED_RADIX = 2;

    static final int MAX_OFFSET = LIT_RADIX * POWERED_RADIX - 1;

    private boolean lit = false;

    private boolean powered = false;

    @ApiStatus.Internal
    public CopperBulbBlockData(int startingID) {
        super(startingID);
    }

    @Override
    public boolean lit() {
        return this.lit;
    }

    @Override
    public void lit(boolean lit) {
        this.lit = lit;
    }

    private int litToIndex() {
        return this.lit ? 0 : 1;
    }

    private void litFromIndex(int index) {
        this.lit(index == 0);
    }

    private void litFromString(String value) {
        this.lit(Boolean.parseBoolean(value));
    }

    @Override
    public boolean powered() {
        return this.powered;
    }

    @Override
    public void powered(boolean powered) {
        this.powered = powered;
    }

    private int poweredToIndex() {
        return this.powered ? 0 : 1;
    }

    private void poweredFromIndex(int index) {
        this.powered(index == 0);
    }

    private void poweredFromString(String value) {
        this.powered(Boolean.parseBoolean(value));
    }

    @Override
    public int maxOffset() {
        return MAX_OFFSET;
    }

    @Override
    int offsetID() {
        int id = 0;
        id = id * LIT_RADIX + litToIndex();
        id = id * POWERED_RADIX + poweredToIndex();
        return id;
    }

    @Override
    void loadFromOffset(int id) {
        poweredFromIndex(id % POWERED_RADIX); id /= POWERED_RADIX;
        litFromIndex(id % LIT_RADIX); id /= LIT_RADIX;
    }

    @Override
    public void load(Map<String, String> properties) {
        if (properties.containsKey("lit")) {
            litFromString(properties.get("lit"));
        }
        if (properties.containsKey("powered")) {
            poweredFromString(properties.get("powered"));
        }
    }

    @Override
    public String toString() {
        return "CopperBulbBlockData[lit=" + String.valueOf(lit).toLowerCase(Locale.ENGLISH) + ";powered=" + String.valueOf(powered).toLowerCase(Locale.ENGLISH) + "]";
    }
}
